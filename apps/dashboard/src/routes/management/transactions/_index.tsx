import { Typography } from '@yukinu/ui/typography'

export default function ManagementTransactionsIndexPage() {
  return (
    <>
      <Typography variant='h2'>Transactions</Typography>
      <Typography className='text-muted-foreground'>
        Manage and view all transactions. View transaction details, filter by
        date, status, and more.
      </Typography>
    </>
  )
}
