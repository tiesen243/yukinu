import { View } from 'react-native'

import { Text } from '@/components/ui/text'

export default function TabsIndexScreen() {
  return (
    <View>
      <Text>
        This is the index screen for the tabs navigator. You can add more
        screens to the tabs navigator by creating new files in the (tabs)
        directory.
      </Text>
    </View>
  )
}
